The bip2ml visualizer is a local webapp that allows users to run the transpiler and, most importantly, navigate a visual representation of the ASTs of both the source BipLang code and the generated OCaml code.
The back-end server was built with Python3 and the front-end with HTML, CSS and Javascript.

Requirements:
- Python3 (to run the server)
