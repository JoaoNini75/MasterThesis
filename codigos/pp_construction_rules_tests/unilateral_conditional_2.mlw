use int.Int

val ref c : bool
val ref x1 : int
val ref x2 : int

let main ()
  requires { c }
  ensures { x1 = x2 }
  =
  assert { c };

  if not c then (
    x1 <- 0; 
    x2 <- 1;
  ) else (
    x1 <- 1;
    x2 <- 1;
  )
  