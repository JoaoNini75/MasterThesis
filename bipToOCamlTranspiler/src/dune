;(ocamllex
 ;(modules lexer))
;(menhir
 ;(modules parser))
;(executable
 ;(name lexer)
; (promote (until-clean))
; (flags ())) 

(ocamllex
 (modules lexer))
(menhir
 (modules parser))
(executable
 (name biplang)
 (promote (until-clean))
 (flags ())
 (modules_without_implementation ast))
