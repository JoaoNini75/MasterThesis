;(ocamllex
;(modules lexer))
;(menhir
;(modules parser))
;(executable
;(name lexer)
; (promote (until-clean))
; (flags ()))

(ocamllex
 (modules lexer))

(menhir
 (modules parser))

(executable
 (name biplang)
 (libraries str)
 (promote (until-clean))
 (flags ()))


