let counting (limit : int <|> limit : int) = begin
	let acc = ref 0 <|> ref 0 in
	let i = 0 in

	while i < limit <|> i < limit do
		acc := !acc + 1 <|> acc := !acc + 2
	done;

	acc := !acc * 2 <|> acc := !acc * 1;
	!acc <|> !acc
end
(*@ ensures true *)
